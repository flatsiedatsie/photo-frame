{
  "author": "Flatsiedatsie",
  "content_scripts": [
    {
      "css": [
        "css/extension.css"
      ],
      "js": [
        "js/extension.js"
      ]
    }
  ],
  "description": "Show your favourite photos",
  "gateway_specific_settings": {
    "webthings": {
      "exec": "python3 {path}/main.py",
      "primary_type": "extension",
      "strict_max_version": "*",
      "strict_min_version": "0.10.0"
    }
  },
  "homepage_url": "https://github.com/flatsiedatsie/photo-frame",
  "id": "photo-frame",
  "license": "MPL-2.0",
  "manifest_version": 1,
  "name": "Photo Frame",
  "options": {
    "default": {
      "Interval": 30,
	  "Screensaver delay": "60",
      "Fit to screen": "mix",
	  "Show clock": true,
	  "Show date": true,
	  "Show weather": true,
      "Debug": false
    },
    "schema": {
      "properties": {
        "Interval": {
          "description": "How many seconds between changing to another random photo.",
          "type": "integer",
		  "minimum": 5,
		  "maximum": 60
        },
        "Screensaver delay": {
          "description": "After how many seconds of inactivity should the photo frame switch itself on as a screensaver? Select 0 if you don't want photo frame to act as a screensaver. The default is 60 seconds. The maximum is 600 (10 minutes)",
          "enum": [
              "0",
              "20",
              "40",
			  "60",
              "120",
			  "180",
              "240",
			  "300",
              "600"
          ],
          "type": "string"
        },
        "Fit to screen": {
          "description": "Pictures might can contained within the screen edges, with black bars around them. Alternatively, pictures could cover the screen fully, but then parts may be cut off. It also possible to mix this randomly.",
          "enum": [
              "Contain",
              "Cover",
              "Mix"
          ],
          "type": "string"
        },
        "Show clock": {
          "description": "Show the current time on top of the photos.",
          "type": "boolean"
        },
        "Show date": {
          "description": "Show the current day and date on top of the photos.",
          "type": "boolean"
        },
        "Show weather": {
          "description": "Show weather data on top of the photos. For this the Weather addon must also be installed and configured with at least one location.",
          "type": "boolean"
        },
        "Debug": {
          "description": "Advanced. Enable this to see much more detail in the internal WebThings Gateway log about what this add-on is doing.",
          "type": "boolean"
        }
      },
      "required": [
        "Interval"
      ],
      "type": "object"
    }
  },
  "short_name": "Photo",
  "version": "1.4.19",
  "web_accessible_resources": [
    "css/*.css",
    "images/*.svg",
    "photos/*.gif",
    "photos/*.png",
    "photos/*.jpg",
    "photos/*.jpeg",
    "js/*.js",
    "views/*.html"
  ]
}
